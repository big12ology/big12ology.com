{
  "name": "b12-pickem-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The pick'em API. The only server-side code on big12ology.com.",
  "scripts": {
    "test": "node --test test/*.test.js",
    "dev": "wrangler dev",
    "migrate:local": "wrangler d1 migrations apply b12_pickem --local",
    "migrate:remote": "wrangler d1 migrations apply b12_pickem --remote",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  }
}
